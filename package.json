{
  "name": "termcade-web-assets",
  "private": true,
  "description": "Tooling only. The engine is Python; this exists so the page's JavaScript is checked by something that reads JavaScript.",
  "scripts": {
    "lint": "eslint engine/termcade/web && prettier --check engine/termcade/web",
    "format": "prettier --write engine/termcade/web"
  },
  "devDependencies": {
    "eslint": "^9.20.0",
    "globals": "^15.15.0",
    "prettier": "^3.5.0"
  }
}
