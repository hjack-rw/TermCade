{
  "name": "termcade-web-assets",
  "private": true,
  "description": "Tooling only. The engine is Python; this exists so the page's JavaScript is checked by something that reads JavaScript.",
  "scripts": {
    "lint": "eslint engine/termcade/web docs/assets && prettier --check engine/termcade/web docs/assets",
    "format": "prettier --write engine/termcade/web docs/assets"
  },
  "devDependencies": {
    "eslint": "^9.20.0",
    "globals": "^15.15.0",
    "prettier": "^3.5.0"
  }
}
