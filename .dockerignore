.git
.venv
__pycache__
*.pyc
*.pyo
.mypy_cache
.ruff_cache
.pytest_cache
tests
docs
dist
build
*.egg-info
.DS_Store
# 24MB of linters for the page assets. Never installed in the image — the Dockerfile copies
# engine/ and games/ by name — but the whole context is sent to the daemon on every build.
node_modules
