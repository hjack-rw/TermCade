"""Persisted, player-facing configuration — what a save keeps across launches.

- :mod:`.settings` — the tunable ruleset (``XiaolinSettings``), player-editable on the Settings screen.
- :mod:`.ladder` — the boss ladder's cleared count, earned by winning rather than chosen.

This package deliberately re-exports nothing — import the submodule you mean.
"""
