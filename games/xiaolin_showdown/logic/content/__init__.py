"""Display/text helpers — how a name or a page of lore is shown, never game rules.

- :mod:`.naming` — the display-name rules (shortening, casing) both screens and the duel log share.
- :mod:`.lore` — the lore book: loading and paginating it.

This package deliberately re-exports nothing — import the submodule you mean.
"""
