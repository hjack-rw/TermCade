"""Behavior that belongs to one specific identity, not a rule the engine applies uniformly.

- :mod:`.jack` — Jack Spicer's own tricks: the bot-swap, the flee, the Yin/Yang flip.
- :mod:`.jong` — Mala Mala Jong, the construct that REPLACES whoever assembles it.

Everything else in ``logic`` treats every character the same way; this package is where the
exceptions live. This package deliberately re-exports nothing — import the submodule you mean.
"""
