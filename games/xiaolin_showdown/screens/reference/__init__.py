"""Reference and configuration screens — no run state of their own, reachable from more than one place.

- :mod:`.rules` — the rulebook.
- :mod:`.lore` — the lore book.
- :mod:`.settings` — edit the ruleset before starting a game.

This package deliberately re-exports nothing — import the submodule you mean.
"""
